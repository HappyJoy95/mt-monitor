# mt-monitor 运行依赖
# 在目标机器上（含 Windows）：
#   python -m venv .venv
#   .venv\Scripts\activate   (Windows)  /  source .venv/bin/activate  (macOS/Linux)
#   pip install -r requirements.txt
playwright>=1.40
requests>=2.28
